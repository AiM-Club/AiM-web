export enum PageEndPoints {
  HOME = "/",
  MYPAGE = "/mypage",
  COMMUNITY = "/community",
  CHALLENGE_MAIN = "/challenge",
  CHALLENGE_VS = "/challenge/vs",
  LOGIN = "/login",
  JOIN = "/join",
  SEARCH = "/search",
}
